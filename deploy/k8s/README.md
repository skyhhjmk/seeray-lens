# Kubernetes

Kubernetes/k3s manifests are intentionally deferred until the server has its first deployable runtime role configuration. Phase 1 keeps Docker Compose as the local dependency environment and records cluster scheduling behaviour in ADR 0005.
